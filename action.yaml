name: My Action
version: v1

inputs:
  message:
    description: 'The message to print'
    required: true

runs-on: windows-latest
runs:
  using: 'bash'
  command: |
    echo "${{ inputs.message }}"
